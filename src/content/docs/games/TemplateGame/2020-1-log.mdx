---
title: "Godot 平台跳跃原型 #1"
description: 使用 Godot 4 开发 Celeste-like 原型：输入、跳跃缓冲、Coyote Time、移动摄像机。
father: "template-game"
lastUpdated: 2025-02-13
tags:
  - design
  - gameplay
  - godot
readTime: "4 min"
giscus: true
---

import { Aside } from '@astrojs/starlight/components';

> 本文档用于**样式演练**：含锚点、旁白、折叠、表格、任务清单、代码组、diff 与行高亮等。
>
> 跳转到 [性能分析](#性能分析) 小节。

![像素风平台跳跃示意](https://images.unsplash.com/photo-1550745165-9bc0b252726f?q=80&w=1600&auto=format&fit=crop&ixlib=rb-4.0.3)
_图 1：原型场景示意_

<span style={{fontWeight:700,color:'var(--color-accent-600)'}}>内联样式示例：</span>此处用自定义色强调关键术语，比如 <kbd>Coyote Time</kbd> 与 _Jump Buffer_。

:::note[目标]
实现最小可玩：**左右移动 + 跳跃 + 缓冲 + 斜坡摩擦**，并输出基础性能指标（FPS/帧时）。
:::

:::tip[输入映射]
在 **Project Settings → Input Map** 增加 `move_left`、`move_right`、`jump`。下面给出 GDScript/C# 双实现。
:::

## 控制器脚本

:::code-group

```gdscript [player.gd] title="player.gd" showLineNumbers {5,16-20}
extends CharacterBody2D

@export var speed := 220.0
@export var jump_velocity := -420.0
@export var coyote_time := 0.08 # [!code highlight]
@export var jump_buffer := 0.12 # [!code highlight]

var _coyote := 0.0
var _buffer := 0.0

func _physics_process(dt: float) -> void:
  var dir := Input.get_axis("move_left", "move_right")
  velocity.x = move_toward(velocity.x, dir * speed, 1600.0 * dt)

  if is_on_floor():
    _coyote = coyote_time # [!code highlight]
  else:
    _coyote = max(_coyote - dt, 0.0)

  if Input.is_action_just_pressed("jump"):
    _buffer = jump_buffer # [!code highlight]
  else:
    _buffer = max(_buffer - dt, 0.0)

  if (_coyote > 0.0) and (_buffer > 0.0):
    velocity.y = jump_velocity
    _buffer = 0.0

  velocity.y += 1200.0 * dt # 重力
  move_and_slide()
```

```csharp [Player.cs] title="Player.cs" showLineNumbers {9,21-25}
using Godot;
public partial class Player : CharacterBody2D
{
  [Export] public float Speed = 220f;
  [Export] public float JumpVelocity = -420f;
  [Export] public float CoyoteTime = 0.08f; // [!code highlight]
  [Export] public float JumpBuffer = 0.12f; // [!code highlight]

  private float coyote, buffer;

  public override void _PhysicsProcess(double dt)
  {
    var x = Input.GetAxis("move_left", "move_right");
    Velocity = new Vector2(Mathf.MoveToward(Velocity.X, (float)(x * Speed), (float)(1600 * dt)), Velocity.Y);

    coyote = IsOnFloor() ? CoyoteTime : Mathf.Max(coyote - (float)dt, 0f);

    if (Input.IsActionJustPressed("jump")) buffer = JumpBuffer;
    else buffer = Mathf.Max(buffer - (float)dt, 0f);

    if (coyote > 0 && buffer > 0)
    {
      Velocity = new Vector2(Velocity.X, JumpVelocity);
      buffer = 0f;
    }

    Velocity += new Vector2(0, (float)(1200 * dt));
    MoveAndSlide();
  }
}
```

### 代码重构（Diff）

```diff title="将移动与跳跃解耦（player.gd）"
- velocity.x = move_toward(velocity.x, dir * speed, 1600.0 * dt)
+ velocity.x = approach(velocity.x, dir * speed, 1600.0 * dt)
+ 
+ func approach(current: float, target: float, delta: float) -> float:
+   if current < target:
+     return min(current + delta, target)
+   return max(current - delta, target)
```

### 旁白示例

<Aside>一些在旁白中的内容。</Aside>

<Aside type="caution">一些警示内容。</Aside>

<Aside type="tip">

旁白中还支持其他内容。

```js
// 例如，代码片段。
```

</Aside>

<Aside type="danger">不要将你的密码告诉任何人。</Aside>

## 性能分析

| 指标 | 数值（Editor） | 说明 |
| --- | :--: | --- |
| 帧时 | 0.9–1.7 ms | 空场景 + 角色控制 |
| Draw Calls | 100–250 | 包含瓦片地图与 UI |
| 内存 | ~120 MB | 含导入贴图缓存 |

- [x] 基本角色控制  
- [x] 缓冲/Coyote Time  
- [ ] 粒子/屏幕冲击波  
- [ ] Boss 原型

<details>
  <summary>展开：摄像机抖动与跟随</summary>
  先用 <code>Camera2D</code> 的 <code>limit</code> 与 <code>offset</code>；后续将引入噪声曲线制造受击抖动。
</details>

### 终端与导出

```bash title="命令行运行 Godot（headless）"
godot --headless --path ./project --quit-after 2
```

```bash title="导出模板（Windows）"
godot --export-release "Windows Desktop" build/game.exe
# [!code warning] 如缺少导出模板请先在 Editor 中下载
```

:::important[版本/素材]
- **Godot 4.3**，GLES3  
- 角色占位图来自开源占位素材包（仅用于原型）
:::
