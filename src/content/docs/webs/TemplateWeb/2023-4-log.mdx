---
title: "Devlog 站点样式演练 #2（组件与可读性）"
description: Tabs/Details/Callouts 组合、字符串标记、混合语法高亮与进一步的 Expressive Code 测试。
father: "template-web"
lastUpdated: 2021-11-03
tags:
  - deprecated
  - migration
  - notes
readTime: "3 min"
giscus: true
---

import { FileTree } from '@astrojs/starlight/components';
import { Steps } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

> 本篇继续覆盖**更多可视元素**，用于全站样式核验。跳转到 [可访问性检查](#可访问性检查)。

![代码与界面示意](https://images.unsplash.com/photo-1515879218367-8466d910aaa4?q=80&w=1600&auto=format&fit=crop&ixlib=rb-4.0.3)
_图 1：代码/界面混排_

:::note
Starlight 支持多种 **Callout**：`note`、`tip`、`warning`、`danger`、`important` 等。
:::

:::danger[谨慎]
在生产站点启用外部脚本时，请开启 CSP 并审核第三方。
:::

## 交互式折叠（Accordion）

<details>
  <summary>为什么我的构建产出与开发预览不同？</summary>
  常见原因：不同 <code>NODE_ENV</code>、Tailwind <code>content</code> 范围漏配、或 CSS 顺序冲突。
</details>

## 文件树

<FileTree>

- astro.config.mjs
- package.json
- src
  - components
    - **Header.astro** 一个 **重要** 文件
    - Title.astro
  - pages/

</FileTree>

## 步骤示例

<Steps>

1. 将组件导入到 MDX 文件中：

   ```js
   import { Steps } from '@astrojs/starlight/components';
   ```

2. 用 `<Steps>` 将你的有序列表包裹起来。

</Steps>

## 选项卡

<Tabs>
  <TabItem label="恒星" icon="star">
    天狼星，织女星，参宿四
  </TabItem>
  <TabItem label="卫星" icon="moon">
    木卫一，木卫二，木卫三
  </TabItem>
</Tabs>

### 同步选项卡

_一些恒星：_

<Tabs syncKey="星座">
  <TabItem label="猎户座">参宿五，参宿七，参宿四</TabItem>
  <TabItem label="双子座">北河三，北河二 A，北河二 B</TabItem>
</Tabs>

_一些系外行星：_

<Tabs syncKey="星座">
  <TabItem label="猎户座">HD 34445 b，格利泽 179b，Wasp-82 b</TabItem>
  <TabItem label="双子座">北河三 b，HAT-P-24b，HD 50554 b</TabItem>
</Tabs>

## 代码块：标题 / 行高亮 / 注释标记

```tsx title="Card.tsx" showLineNumbers {3,9,16}
export function Card({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <div className="rounded-2xl shadow-lg p-6 bg-card/60 backdrop-blur">
      <h3 className="text-xl font-semibold">{title}</h3> {/* [!code highlight] */}
      <div>{children}</div>
    </div>
  )
}
// TODO: 支持 asChild 渲染  // [!code warning]
```

```ts title="搜索关键字高亮（简单实现）" showLineNumbers {5}
export function mark(text: string, q: string) {
  if (!q) return text;
  const re = new RegExp(`(${q.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")})`, "ig");
  return text.replace(re, "<mark>$1</mark>"); // [!code highlight]
}
```

### 混合语法高亮（CSS-in-JS / Tailwind 类）

```tsx title="Badge.tsx" showLineNumbers {6,12}
export const Badge = ({ ok = false }) => (
  <span
    className={
      "inline-flex items-center px-2 py-0.5 rounded " +
      (ok ? "bg-green-500/20 text-green-300" : "bg-yellow-500/20 text-yellow-300") // [!code highlight]
    }
  >
    {ok ? "OK" : "Pending"}
  </span>
);
```

### 代码组（多语言切换）

:::code-group

```ts [utils.ts] title="utils.ts"
export const clamp = (n: number, a: number, b: number) => Math.min(b, Math.max(a, n));
```

```py [utils.py] title="utils.py"
def clamp(n: float, a: float, b: float) -> float:
    return min(b, max(a, n))
```

```rust [utils.rs] title="utils.rs"
pub fn clamp(n: f64, a: f64, b: f64) -> f64 { b.min(a.max(n)) }
```

:::

### Diff：Tailwind 类名收敛

```diff title="将重复类名提取为 @apply"
-.btn { @apply px-3 py-1 rounded-lg bg-purple-600 text-white hover:bg-purple-500; }
+.btn { @apply px-3 py-1 rounded-lg bg-accent-600 text-white hover:bg-accent-600/90; }
```

### 终端：脚本与输出

```bash title="清空产物并重建"
rimraf .astro dist && astro build
# 输出略……
```

## 可访问性检查

| 检查项 | 结果 | 说明 |
| --- | :--: | --- |
| 对比度 | ✅ | 文字≥4.5:1 |
| 焦点环 | ✅ | 自定义 `:focus-visible` |
| 语义标签 | ✅ | 按钮/链接语义明确 |

- [x] 统一按钮风格  
- [x] 代码组语言标签可见  
- [ ] 暗色模式下图片偏灰（待调 curves）

> 额外：可以用 `<span style={{color:'var(--color-accent-600)'}}>自定义色</span>` 对行内关键字着色；  
> 对被删除/新增的文案可用 `<del>删除</del>` / `<ins>新增</ins>` 做“字符串标记”。

:::important[结论]
此篇聚焦**可读性与一致性**，下一篇将验证搜索与分页的交互状态样式。
:::

